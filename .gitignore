# Terraform
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
*.tfplan
tfplan
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# Sensitive variable files
*.tfvars
*.tfvars.json
!example.tfvars

# OS files
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo

# Env files
.env
.env.*

# =============================================================================
# Security - Sensitive Files
# =============================================================================
# NEVER commit these files - they contain secrets or credentials

# Ziti enrollment tokens (one-time use, contain identity info)
*.jwt

# TLS/SSL certificates and private keys
*.pem
*.key
*.cert
*.crt
*.p12
*.pfx

# Secrets directories
**/secrets/
**/.secrets/

# Ansible vault files (if using plaintext vault password)
.vault_pass
.vault_password
vault_password_file

# AWS credentials (should use SSO/profiles instead)
credentials
.aws/credentials

# SSH keys
id_rsa
id_ed25519
*.pem

# Ansible inventory cache
/tmp/aws_ec2_cache

# Terraform variables with secrets
*.tfvars
!*.tfvars.example
.provision-basic-server-cache
