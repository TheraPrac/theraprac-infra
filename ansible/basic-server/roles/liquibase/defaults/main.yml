---
# =============================================================================
# Liquibase Role Defaults
# =============================================================================

# Liquibase version
liquibase_version: "4.29.2"

# Installation directory
liquibase_install_dir: /opt/liquibase

# PostgreSQL JDBC driver version
postgres_jdbc_version: "42.7.4"

# Symlink location
liquibase_bin: /usr/local/bin/liquibase


